// Crear las variables 'x' y 'y' y asignarles los valores 2 y 3 respectivamente
var x = 2;
var y = 3;

// Calcular la suma de 'x' y 'y'
var suma = x + y;

// Mostrar el resultado de la suma en el elemento con id 'sum'
document.getElementById('sum').textContent = suma;
