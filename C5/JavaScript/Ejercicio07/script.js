// Crear las variables
var x = 1;
var y = parseInt("6");  // Convertir la cadena "6" a un número
var z = x + y;

// Mostrar el resultado en el elemento con id 'donde'
document.getElementById("donde").innerHTML = z;
