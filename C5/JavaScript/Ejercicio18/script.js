// Utiliza Math.max() para encontrar el número más alto
var numeroMasAlto = Math.max(0, 150, 30, 20, -8);

// Mostrar el número más alto en el elemento con id 'demo'
document.getElementById("demo").innerHTML = "El número más alto es: " + numeroMasAlto;
